# 2026-07-05 工作日志

## 重写 README.md
- 原始 README 严重缺失生产级架构描述：完全没有提到 MySQL 持久化、Redis 缓存、RabbitMQ 队列、Worker 进程、用户系统、前端、完整 API 列表
- 本地端口不准确（原文写 8000，实际 server.py 用 8001）；POST /api/research 描述为 SSE 流式，实际是异步队列模式
- 重写后补充：生产部署架构图、完整 API 接口表（注册/登录/游客/用户/调研/历史/报告/健康检查）、环境变量表、4 张数据表说明、Docker Compose 部署说明、Redis/RabbitMQ 缓存与队列特性、ContextVar 用户隔离、前端 /ui 挂载
- 保留原有正确的部分：Agent 流水线图、核心特性、State 总览、测试表、项目结构（已补全 db/cache/queue/core/frontend/worker）

## README 图片验证
- 用户要求验证 docs/images/ 下 9 张截图内容是否与 README 描述匹配
- 当前模型不支持图片读取（multimodal 限制），9 张 PNG 全部读取失败
- 替代方案：通过分析 frontend/index.html 源码，为每张图生成了精确到 UI 元素级别的对照清单
- 清单覆盖：顶部栏（渐变标题+用户徽章）、左侧栏（历史对话）、标签栏、输入区（input+下拉+按钮）、状态栏（圆点+计时+轮次+证据+质量）、进度面板（4 节点执行轨迹）、报告面板（Markdown 渲染）、终端测试输出
- 结论：9 张图覆盖完整使用流程（空首页→示例→输入→搜索→评估→生成→报告→结果→测试），逻辑完整无缺失场景

## 删除界面截图
- 用户要求删除所有界面截图，只保留 test-pytest.png
- 已删除 8 个文件：homepage-empty/examples/input、progress-searching/critic/synthesizer、report-full/result
- README 界面截图部分替换为「截图获取指南」：含启动方式（Docker/本地）、8 张截图的操作步骤表（文件名+操作+应捕捉画面）、截图技巧
- 截图指南基于 frontend/index.html 源码精确编写，每张图标注了应出现的 UI 元素
